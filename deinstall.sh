#!/usr/bin/env bash

rm -fr \
    ~/.dotfiles \
    ~/.oh-my-zsh \
    ~/.tmux \
    ~/.config/nvim \
    ~/.local/state/nvim \
    ~/.local/share/nvim

rm -f ~/.tmux.conf
rm -f ~/.zshrc
rm -f ~/.tmux.conf
rm -f ~/.gitconfig
rm -f ~/.config/starship.toml
